{% extends 'layouts/app.html.twig' %}

{% block content %}
    <div class="space-y-6" data-dashboard="1">
        {# KPI Cards (valeurs chargées via /api/dashboard/summary) #}
        <section class="grid grid-cols-1 md:grid-cols-4 gap-4">
            <div class="bg-white rounded-xl border border-gray-200 p-5 flex items-center justify-between">
                <div>
                    <p class="text-xs tracking-wide text-gray-500 uppercase">Recettes</p>
                    <p class="mt-2 text-3xl font-bold text-dark-blue" id="kpi-recipes">—</p>
                </div>
                <div class="w-10 h-10 rounded-lg bg-light-blue flex items-center justify-center">
                    <span class="text-dark-blue font-bold">📚</span>
                </div>
            </div>

            <div class="bg-white rounded-xl border border-gray-200 p-5 flex items-center justify-between">
                <div>
                    <p class="text-xs tracking-wide text-gray-500 uppercase">À venir</p>
                    <p class="mt-2 text-3xl font-bold text-dark-blue" id="kpi-upcoming">—</p>
                    <p class="mt-1 text-xs text-gray-500" id="kpi-period">—</p>
                </div>
                <div class="w-10 h-10 rounded-lg bg-light-blue flex items-center justify-center">
                    <span class="text-dark-blue font-bold">🗓️</span>
                </div>
            </div>

            <div class="bg-white rounded-xl border border-gray-200 p-5 flex items-center justify-between">
                <div>
                    <p class="text-xs tracking-wide text-gray-500 uppercase">Favoris</p>
                    <p class="mt-2 text-3xl font-bold text-dark-blue" id="kpi-favorites">—</p>
                </div>
                <div class="w-10 h-10 rounded-lg bg-light-blue flex items-center justify-center">
                    <span class="text-dark-blue font-bold">❤️</span>
                </div>
            </div>

            <div class="bg-white rounded-xl border border-gray-200 p-5 flex items-center justify-between">
                <div>
                    <p class="text-xs tracking-wide text-gray-500 uppercase">Dernière</p>
                    <p class="mt-2 text-lg font-bold text-dark-blue" id="kpi-latest">—</p>
                    <p class="mt-1 text-xs text-gray-500" id="kpi-latest-date"></p>
                </div>
                <div class="w-10 h-10 rounded-lg bg-light-blue flex items-center justify-center">
                    <span class="text-dark-blue font-bold">⏱️</span>
                </div>
            </div>
        </section>
    </div>
{% endblock %}

