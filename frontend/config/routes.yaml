# ============================================
# Routes centralisées de l'application
# ============================================
# Toutes les routes sont définies via les attributs #[Route] dans les contrôleurs
# Ce fichier importe automatiquement tous les contrôleurs

controllers:
    resource: ../src/Controller/
    type: attribute
